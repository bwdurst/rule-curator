---
name: prefer-pnpm
description: use pnpm, not npm, for installs and scripts
metadata:
  type: feedback
---

Use pnpm for all package operations (install, run scripts, add/remove deps).
pnpm's content-addressed store is faster and avoids phantom dependencies.
